https://learn.unity.com/pathway/unity-essentials

# 🎮 Unity Shortcuts Reference

## 🧭 Scene View Navigation

- **View (시점 이동)**  
  - 마우스 오른쪽 버튼 누른 채 드래그

- **Frame (프레임 맞추기)**  
  - Scene View에서 `F` 키  
  - Hierarchy 창에서 GameObject 더블 클릭

- **Orbit (회전)**  
  - `Alt` (macOS: `Option`) + 마우스 왼쪽 버튼 드래그

- **Zoom (확대 / 축소)**  
  - 마우스 휠 스크롤  
  - `Alt` (macOS: `Option`) + 마우스 오른쪽 버튼 드래그

- **Flythrough Mode (자유 이동 모드)**  
  - 마우스 오른쪽 버튼 누른 상태에서 이동  
    - `W / A / S / D` : 이동  
    - `Q` : 아래로 이동  
    - `E` : 위로 이동

---

## 🛠 Scene View Tools Shortcuts

| 기능 | 단축키 |
|---|---|
| View | `Q` |
| Move | `W` |
| Rotate | `E` |
| Scale | `R` |
| Rect | `T` |
| Transform | `Y` |

---

## ⚙ Miscellaneous Shortcuts

- **Undo (실행 취소)**  
  - `Ctrl + Z` (macOS: `Cmd + Z`)

- **Save (저장)**  
  - `Ctrl + S` (macOS: `Cmd + S`)

---

